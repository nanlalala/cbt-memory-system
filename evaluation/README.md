# Evaluation

The main study will compare no cross-session memory, short-term memory only, and short-term plus manageable long-term memory using the same frozen RAG and response model.

Metrics include memory storage/retrieval correctness, temporal/latest-state accuracy, contradiction and hallucination rate, assignment tracking, response consistency, safety routing, latency, token cost and user experience.

